using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Controls : MonoBehaviour
{
    // Declare inventory objects
    // Declare Inventory storage space

    // Initialize the storage area
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // Update when the player does an interaction

        // if player presses e
            // if item nearby: initialize object and store
            // else if no item nearby: do nothing
            
            // if NPC nearby: start interaction

        // if player presses spacebar and nearby shadow: start shadow technique

        // if player presses LMB: Kill/shoot/throw
        // if player presses RMB: KO/Aim
        // if player presses right click: lock aim
    }
}
